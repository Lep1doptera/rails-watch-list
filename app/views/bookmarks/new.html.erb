<div class="container">
  <div class="row justify-content-center m-3 ">
    <div class="col-md-6 bg-light p-5 shadow-sm">
      <h1>Add a movie 🎬 </h1>
      <%= simple_form_for [@list, @bookmark] do |f| %>
        <%= f.input :movie_id, collection: Movie.all.map { |m| [m.title, m.id] }, prompt: "Choose a movie" %>
        <%= f.input :comment %>
        <%= f.submit class: "btn btn-outline-primary"%> <%= link_to "Cancel", lists_path, class: "btn btn-outline-danger"%>
      <% end %>
    </div>
  </div>
</div>
